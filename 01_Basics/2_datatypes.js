"use strict"; //treats all JS code as newer version

// alert(3+3) --> we are using nodeJS, not the browser so this 'alert' won't work here 

// code readability must be the priority

let name = "Ansh"
let age = 21
let isLoggedIn = false

/* Notes
>number range = 2^53
>bigint (for very big number)
>string  
>boolean (yes/no)
>null (is actually a stand alone value, a representation of an empty value)
>undefined (when value is not defined)
>symbol(used in react, used for determining uniqueness)
>object(): output of typeof
typeof is used to determine the type of the datatype 
Ex: console.log(typeof undefined); --> undefined
console.log(typeof null); --> object */